<Window x:Class="Filmator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:r="clr-namespace:Filmator.Resources.View"
        xmlns:UserControls="clr-namespace:Filmator.View.UserControls"
        xmlns:Converters="clr-namespace:Filmator.Converters"
        xmlns:ignore="http://www.ignore.com" mc:Ignorable="d ignore"
        Height="600"
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="{x:Static r:MainWindow.Title}"
        DataContext="{Binding Main, Source={StaticResource Locator}}" FontWeight="Bold">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins\MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="Layout">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="75" />
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <!-- Row 0 -->
        <UserControls:Header Grid.Row="0"/>

        <!-- Row 1 -->
        <UserControls:NavigationBar Grid.Row="1"/>

        <!-- Row 2 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"-->

            <!-- Column 0 -->
            <UserControls:MovieList Grid.Column="0" Visibility="{Binding MovieListVisibility}"/>
            
            <!-- Column 1 -->
            <UserControls:SelectedMovie Grid.Column="1" Visibility="{Binding SelectedMovieVisibility}"/>

            <UserControls:GenreList Grid.Column="1" Visibility="{Binding GenreListVisibility}"/>
        </Grid>

        <!-- Row 3 -->
    </Grid>
</Window>
